# Frontend environment (public). Copy to `.env` for local overrides.

# Where the Vite dev server proxies `/api` requests. Defaults to the local
# NestJS backend. Only used in development; production serves the SPA behind
# the same origin as the API (configured in Phase 18).
VITE_API_PROXY_TARGET=http://localhost:3000
